import styled from "styled-components";

const StyledConvertorWrapper = styled.div`
  padding: 25px;
  .grid-wrapper {
    display: flex;
    .grid-item div {
      width: 100%;
      height: 50px;
      border: 1px solid red;
    }
  }
`;

export default StyledConvertorWrapper;
